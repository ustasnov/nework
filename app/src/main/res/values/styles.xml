<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Widget.AppTheme.FavoriteButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_favorite</item>
        <item name="iconTint">@color/color_favorite</item>
        <item name="android:textColor">@color/ext_gray</item>
        <item name="android:minWidth">@dimen/avatar_size</item>
        <item name="android:minHeight">@dimen/avatar_size</item>
    </style>

    <style name="Widget.AppTheme.ShareButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_baseline_share_24</item>
        <item name="iconTint">@color/color_favorite</item>
        <item name="android:textColor">@color/color_favorite</item>
        <item name="android:minWidth">@dimen/avatar_size</item>
        <item name="android:minHeight">@dimen/avatar_size</item>
    </style>

    <style name="Widget.AppTheme.ViewButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_baseline_remove_red_eye_24</item>
        <item name="iconTint">@color/color_favorite</item>
        <item name="android:textColor">@color/color_favorite</item>
        <item name="android:minWidth">@dimen/avatar_size</item>
        <item name="android:minHeight">@dimen/avatar_size</item>
    </style>

    <style name="Widget.AppTheme.MenuButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="icon">@drawable/ic_baseline_more_vert_24</item>
        <item name="iconTint">@color/color_favorite</item>
        <item name="android:minWidth">@dimen/avatar_size</item>
        <item name="android:minHeight">@dimen/avatar_size</item>
    </style>

</resources>